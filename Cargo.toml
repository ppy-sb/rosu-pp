[package]
name = "rosu-pp"
version = "4.0.1"
edition = "2024"
authors = ["MaxOhn <ohn.m@hotmail.de>"]
license = "MIT"
readme = "README.md"
repository = "https://github.com/MaxOhn/rosu-pp"
documentation = "https://docs.rs/rosu-pp/"
description = "Difficulty and performance calculation for osu!"
keywords = ["osu", "pp", "stars", "performance", "difficulty"]

[features]
default = []
sync = []
tracing = ["rosu-map/tracing"]
reports = ["dep:rayon", "dep:comfy-table", "dep:clap"]

[dependencies]
divan = "0.1.21"
rosu-map = { version = "0.2.1" }
rosu-mods = { version = "0.4.0" }
rayon = { version = "1.11", optional = true }
comfy-table = { version = "8.0", optional = true }
clap = { version = "4.5", features = ["derive"], optional = true }

[dev-dependencies]
comfy-table = "7.1"
divan = { version = "0.1.21" }
dhat = "0.3"
proptest = { version = "1.8.0" }
rayon = "1.11"
rosu-mods = { version = "0.4.0", features = ["macros"] }
xz2 = "0.1"

[profile.test.package.proptest]
opt-level = 3

[profile.test.package.rand_chacha]
opt-level = 3

[[bin]]
name = "multiuser_report"
required-features = ["reports"]

[[bin]]
name = "surface_dump"
required-features = ["reports"]

[[bench]]
name = "hitresult_generators"
harness = false
